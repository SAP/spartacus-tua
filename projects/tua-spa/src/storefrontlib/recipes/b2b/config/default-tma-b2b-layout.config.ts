import { LayoutConfig } from '@spartacus/storefront';

export const defaultTmaB2bLayoutConfig: LayoutConfig = {
  layoutSlots: {
    GuidedSellingPageTemplate: {
      slots: [
        'GuidedSellingContentSlot',
      ],
    },
  },
};
