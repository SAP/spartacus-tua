import { TmfConfig } from '../../config/tmf-config';

export const defaultTmaTmfChecklistActionConfig: TmfConfig = {
  backend: {
    tmf: {
      endpoints: {
        checklistAction:
          'checklistAction',
      },
    },
  },
};
